<% if (success && success.length){ %>
  <div class="alert alert-success " role="alert">
    <strong>
      <%= success %>
    </strong>
    </button>
  </div>
  <% } %>

    <% if (error && error.length){ %>
      <div class="alert alert-warning " role="alert">
        <strong>
          <%= error %>
        </strong>
        </button>
      </div>
      <% } %>


        <% if (updated && updated.length){ %>
          <div class="alert alert-primary" role="alert">
            <strong>
              <%= updated %>
            </strong>
            </button>
          </div>
          <% } %>


            <!-- copied from the bootstrap webiste from the alerts section to push an alert for the when the listing added ,deleted or updated  -->